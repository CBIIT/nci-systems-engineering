---
# tasks file for refresh-db

- include_vars: ../../vars.yml

#- name: Taking database dump of {{ sitename }} {{ tier }}
#  shell: drush sql-dump | ssh {{ thost }} "cd {{ drupalroot }} && vendor/bin/drush sql:cli"

- name: Set authorized key taken from file
  authorized_key:
    user: '{{ sshuser }}'
    state: present
    key: '{{ jenkins_key }}'

- name: Dud in fhost
  shell: ssh {{ sshuser }}:{{ fhost }} "touch test.dud"

#- name: Check the ssh env vars
#  debug: '{{ sshuser }}'

#- name: Transfer file from ServerA to ServerB
#  synchronize:
#    src: /home/ncidrupal/delete.dud
#    dest: /home/ncidrupal/delete.dud
#    rsync_opts: "-e ssh -p 22"
#  delegate_to: '{{ fhost }}'

#- name: Rsync to thost
#  shell: rsync -a delete.dud {{ thost }}:{{ drupalroot }}/

#- name: rsync the existing webdirectory against the target directory
#  shell: rsync -a --delete --exclude web/sites/default/settings.php web {{ thost }}:{{ drupalroot }}/
